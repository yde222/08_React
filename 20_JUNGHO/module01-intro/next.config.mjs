/* 
  next.config.mjs 파일은 Next.js 애플리케이션의 설정파일이다.
  이 파일은 Next.js 애플리케이션의 설정을 정의하고, 애플리케이션의 동작을 제어하는데 사용된다.
*/

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
